---
phase: 05-pii-masking-hardening-and-test-coverage
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - server/tests/pii.unit.test.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "All piiNamePatterns entries in the array (currently 29) are individually verified by isLikelyPiiColumn tests"
    - "Every buildDummyValue branch (email, phone, first_name, last_name, full_name, name, address, city, state, zip, country, username, password/token/secret, ssn, dob/birth, passport, string fallback, number fallback, bigint fallback, boolean fallback, null passthrough) has a passing test"
    - "sanitizeSamples masks a multi-column mock table containing ssn/dob/passport/email columns and leaves non-PII columns untouched"
    - "generateTableDescriptions returns {} and does not throw when JSON.parse receives malformed input"
    - "npm test (node --test) exits zero with all pii.unit.test.js tests passing"
  artifacts:
    - path: "server/tests/pii.unit.test.js"
      provides: "Unit tests for isLikelyPiiColumn, buildDummyValue, sanitizeSamples, generateTableDescriptions parse failure"
      min_lines: 120
  key_links:
    - from: "server/tests/pii.unit.test.js"
      to: "server/services/postgres.service.js"
      via: "named ES module imports: isLikelyPiiColumn, buildDummyValue, sanitizeSamples"
      pattern: "import.*postgres.service"
---

<objective>
Create server/tests/pii.unit.test.js covering all piiNamePatterns (TEST-01), every buildDummyValue branch (TEST-02), full sanitizeSamples masking behaviour (TEST-03), and the generateTableDescriptions JSON parse failure path (PII-04). Uses Node's built-in test runner (node:test / node:assert) — the same runner used by all other test files in this project.

Purpose: Prove the PII masking layer behaves correctly after the fixes in Plan 01.
Output: server/tests/pii.unit.test.js with 30+ passing test cases, zero failures.
</objective>

<execution_context>
@C:/Users/Doaa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Doaa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/services/postgres.service.js
@server/tests/cache.test.js
</context>

<interfaces>
<!-- Exports available from postgres.service.js after Plan 01 fixes -->
<!-- These are pure functions — no DB connection needed for unit tests -->

```javascript
// Named exports to import in tests:
import {
  sanitizeSamples,
  isLikelyPiiColumn,
  buildDummyValue,
  generateTableDescriptions,
} from '../services/postgres.service.js';
```

NOTE: isLikelyPiiColumn and buildDummyValue are added to exports in Plan 01 (Task 1 action).
generateTableDescriptions is already exported and needed for the PII-04 test.

<!-- schemaRows shape expected by sanitizeSamples / isLikelyPiiColumn: -->
```javascript
const schemaRows = [
  { table_name: 'users', column_name: 'id',       is_primary: true,  data_type: 'integer' },
  { table_name: 'users', column_name: 'ssn',      is_primary: false, data_type: 'integer' },
  { table_name: 'users', column_name: 'dob',      is_primary: false, data_type: 'date'    },
  { table_name: 'users', column_name: 'passport', is_primary: false, data_type: 'text'    },
  { table_name: 'users', column_name: 'email',    is_primary: false, data_type: 'text'    },
  { table_name: 'users', column_name: 'score',    is_primary: false, data_type: 'integer' },
];
```
</interfaces>

<feature>
  <name>PII unit tests</name>
  <files>server/tests/pii.unit.test.js, server/services/postgres.service.js</files>
  <behavior>
    RED phase: write ALL tests first, run — they should fail (isLikelyPiiColumn / buildDummyValue not exported yet, and PII-01/02/03 fixes not yet present if running before Plan 01, but Plan 01 depends_on means fixes ARE present).

    Actually: Plan 02 depends_on Plan 01, so fixes ARE in place. Write tests that verify the FIXED behaviour. Tests should pass GREEN immediately after writing if Plan 01 was executed correctly.

    Test cases to include:

    === describe('isLikelyPiiColumn') ===

    All piiNamePatterns entries — one it() each, passing a column name containing that pattern.
    Count the actual entries in the piiNamePatterns array in postgres.service.js (currently 29):
    'name', 'email', 'phone', 'mobile', 'ssn', 'social_security', 'passport',
    'first_name', 'lastname', 'last_name', 'fullname', 'full_name', 'middle_name',
    'dob', 'birth', 'address', 'street', 'city', 'state', 'zip', 'postal',
    'country', 'username', 'user_name', 'password', 'passcode', 'token', 'secret', 'api_key'
    Test every entry — if the array changes, update the test count to match.

    Additional behaviour tests:
    - primary key column returns false even if name matches (is_primary: true)
    - dob with data_type='date' returns TRUE (key regression test for PII-02 fix)
    - birth_date with data_type='date' returns TRUE
    - created_at with data_type='date' returns FALSE (non-PII date column stays unmasked)
    - value that looks like email triggers true even with non-PII column name
    - value that looks like phone triggers true

    === describe('buildDummyValue') ===

    One it() per branch:
    - email branch: buildDummyValue('email', 'real@test.com', 0) === 'user1@example.com'
    - phone branch: buildDummyValue('phone', '555-1234', 0) === '5550100001' (or similar padded)
    - mobile branch: buildDummyValue('mobile', '555-1234', 1) uses phone pattern
    - first_name: buildDummyValue('first_name', 'Alice', 0) === 'FirstName1'
    - last_name: buildDummyValue('last_name', 'Smith', 0) === 'LastName1'
    - lastname: buildDummyValue('lastname', 'Jones', 0) === 'LastName1'
    - full_name: buildDummyValue('full_name', 'Alice Smith', 0) === 'Person 1'
    - fullname: buildDummyValue('fullname', 'Bob', 0) === 'Person 1'
    - name (exact): buildDummyValue('name', 'Alice', 0) === 'Name1'
    - address: buildDummyValue('address', '123 Main', 0) === '101 Example St'
    - street: buildDummyValue('street', '1 Oak', 0) includes 'Example St'
    - city: buildDummyValue('city', 'NYC', 0) === 'City1'
    - state: buildDummyValue('state', 'NY', 0) === 'State1'
    - zip: buildDummyValue('zip', '10001', 0) === '00001'
    - postal: buildDummyValue('postal', 'SW1A', 0) === '00001'
    - country: buildDummyValue('country', 'USA', 0) === 'Country1'
    - username: buildDummyValue('username', 'alice', 0) === 'user_1'
    - user_name: buildDummyValue('user_name', 'alice', 0) === 'user_1'
    - password: buildDummyValue('password', 'secret', 0) === 'redacted_1'
    - token: buildDummyValue('token', 'abc123', 0) === 'redacted_1'
    - secret: buildDummyValue('secret', 'key', 0) === 'redacted_1'
    - api_key: buildDummyValue('api_key', 'sk-xxx', 0) === 'redacted_1'
    - ssn (string): buildDummyValue('ssn', '123-45-6789', 0) === '***-**-****'
    - ssn (integer): buildDummyValue('ssn', 123456789, 0) === '***-**-****'
    - social_security (bigint): buildDummyValue('social_security', 987654321n, 0) === '***-**-****'
    - dob (string): buildDummyValue('dob', '1985-06-15', 0) === '1900-01-01'
    - birth_date: buildDummyValue('birth_date', '1990-01-01', 0) === '1900-01-01'
    - passport: buildDummyValue('passport', 'AB123456', 0) === 'redacted'
    - passport_number: buildDummyValue('passport_number', 'XY789', 0) === 'redacted'
    - null passthrough: buildDummyValue('ssn', null, 0) === null
    - undefined passthrough: buildDummyValue('ssn', undefined, 0) === undefined
    - generic string fallback: buildDummyValue('notes', 'some text', 0) === 'redacted_1'
    - generic number fallback: buildDummyValue('score', 99, 0) === 1
    - bigint fallback: buildDummyValue('count', 100n, 0) equals BigInt(1)
    - boolean fallback: buildDummyValue('active', true, 0) === false

    === describe('sanitizeSamples') ===

    - masks ssn integer column: rows[0].ssn === '***-**-****'
    - masks dob date column: rows[0].dob === '1900-01-01'
    - masks passport text column: rows[0].passport === 'redacted'
    - masks email column: rows[0].email matches /user\d+@example\.com/
    - leaves non-PII column untouched: rows[0].score === original value
    - leaves primary key untouched: rows[0].id === original value
    - maskedColumns array includes 'ssn', 'dob', 'passport', 'email'
    - maskedColumns array does NOT include 'id', 'score'
    - handles empty tableSamples gracefully (returns empty sanitized object)

    === describe('generateTableDescriptions — JSON parse failure (PII-04)') ===

    - When the OpenAI response content is malformed JSON (e.g. 'NOT VALID JSON {{{'), the function
      must return {} without throwing.
    - Strategy: generateTableDescriptions accepts an openai client as a parameter. Pass a stub
      client whose chat.completions.create resolves with a mock response containing malformed JSON
      as the message content. Assert the return value deepEquals {}.
    - If the function signature does not accept an injected client (it uses the module-level openai
      instance), use module mocking or simply grep the source to confirm the try/catch exists and
      write an it() that documents the expected contract rather than executing the live function.
      Either approach is acceptable — the goal is a named test entry for this behaviour so a future
      regression is caught.
  </behavior>
  <implementation>
    1. isLikelyPiiColumn and buildDummyValue are already added to named exports in Plan 01 (Task 1 action). Import them along with sanitizeSamples and generateTableDescriptions.
    2. Create server/tests/pii.unit.test.js using node:test / node:assert/strict pattern
    3. Use describe/it blocks matching the existing test file style (see cache.test.js)
    4. No mocking needed for pure functions — these are pure functions
    5. For the generateTableDescriptions PII-04 test, see behavior block for mocking strategy
    6. Run: cd server && node --test tests/pii.unit.test.js
  </implementation>
</feature>

<verification>
```bash
cd /c/Users/Doaa/Documents/GitHub/ai-db-explorer-2026/server && node --test tests/pii.unit.test.js
```

Expected output: all tests pass (ok), 0 failures, exit code 0.

Then run full suite to confirm no regressions:
```bash
cd /c/Users/Doaa/Documents/GitHub/ai-db-explorer-2026/server && node --test tests/*.test.js
```
</verification>

<success_criteria>
- server/tests/pii.unit.test.js exists with 35+ test cases
- All piiNamePatterns entries are tested individually in isLikelyPiiColumn describe block (count matches the actual array length in postgres.service.js)
- Every buildDummyValue branch has a named test including ssn-as-integer, dob, and passport
- sanitizeSamples integration tests cover masking and non-masking columns
- generateTableDescriptions JSON parse failure (PII-04) has a named test case
- node --test tests/pii.unit.test.js exits 0 with no failures
- Full test suite (node --test tests/*.test.js) still passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-pii-masking-hardening-and-test-coverage/05-02-SUMMARY.md`
</output>
